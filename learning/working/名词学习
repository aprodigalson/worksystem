智能运维： 算法+运维
例子：
关联根因分析：利用系统的运维信息如操作日志，事件，告警，计数器等，进行问题的根因分析。
日志的智能分析： 要求（日志的格式要标准）

webhook:http回调，利用一个反向的URL来获取信息的实时改变。
一般情况下，如果A要得到B的最新状态，A通过轮询B的方式来获取最新的状态  A ---》 B
webhook机制则需要A提供一个API ，让B发生变更时主动调用A提供的API接口   A 《---- B
有点像是回调函数， 也有点像注册订阅。

